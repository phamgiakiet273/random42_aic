# Tunnel agent config: connect through ngrok's Tokyo edge (docker-compose-gateway.yml).
# The authtoken comes from NGROK_AUTHTOKEN, not from this file.
version: 3
agent:
  connect_url: connect.jp.ngrok-agent.com:443
